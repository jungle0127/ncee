server:
  port: 8080
  servlet:
    contextPath: /ncee
management:
  port: 8081
  health:
    mail:
      enabled: false
spring:
  datasource:
    name: primaryDataSource
    username: root
    password: lotus
    url: jdbc:mysql://127.0.0.1:3306/ncee?sessionVariables=foreign_key_checks=0&serverTimezone=CTT&useSSL=false&useUnicode=true&characterEncoding=utf8&autoReconnect=true&failOverReadOnly=false&maxReconnects=10
    #close foreign key checking for unit test    
    driver-class-name: com.mysql.jdbc.Driver
  banner:
    charset: UTF-8 # Banner file encoding.
    location: classpath:banner/banner.txt # Banner text resource location.
    image:
      location: classpath:banner/banner.png 
      # Banner image file location (jpg or png can also be used).
      width: 70 # Width of the banner image in chars.
      height: 20 # Height of the banner image in chars (default based on image height).
      margin: 2 # Left hand image margin in chars.
      invert: true # Whether images should be inverted for dark terminal themes.
mybatis:
  config-location: classpath:mybatis.cfg.xml